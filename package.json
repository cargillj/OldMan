{
  "name": "old-man-bot",
  "version": "0.0.1",
  "description": "I'm just an old bot, afraid of change.",
  "main": "src/bot.ts",
  "author": "cargillj",
  "watch": {
    "start": {
      "patterns": [
        "src"
      ]
    }
  },
  "scripts": {
    "pre-commit-message": "echo Performing pre-commit checks... && exit 0",
    "build": "tsc -p .",
    "clean": "rimraf build",
    "start": "rimraf build && tsc -p . && node build/bot.js",
    "test": "jest",
    "watch": "npm-watch"
  },
  "jest": {
    "roots": [
      "src"
    ],
    "verbose": true
  },
  "pre-commit": [
    "pre-commit-message",
    "test",
    "build"
  ],
  "dependencies": {
    "@types/node": "^12.11.7",
    "discord.io": "https://github.com/woor/discord.io/tarball/gateway_v6",
    "moment": "^2.24.0",
    "typescript": "^3.6.4",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3",
    "@babel/preset-typescript": "^7.6.0",
    "@types/jest": "^24.0.21",
    "babel-jest": "^24.9.0",
    "jest": "^24.9.0",
    "npm-watch": "^0.6.0",
    "pre-commit": "^1.2.2",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.1.0"
  }
}
